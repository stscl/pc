% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pc.R
\name{pc}
\alias{pc}
\alias{pc,data.frame-method}
\alias{pc,sf-method}
\alias{pc,SpatRaster-method}
\title{Pattern Causality}
\usage{
\S4method{pc}{data.frame}(
  data,
  target,
  source,
  libsizes = NULL,
  E = 3,
  k = E,
  tau = 1,
  style = 1,
  lib = NULL,
  pred = NULL,
  boot = 99,
  replace = FALSE,
  seed = 42L,
  dist.metric = c("euclidean", "manhattan", "maximum"),
  zero.tolerance = max(k),
  relative = TRUE,
  weighted = TRUE,
  threads = length(libsizes),
  higher.parallel = TRUE,
  verbose = TRUE,
  h = 0,
  ...
)

\S4method{pc}{sf}(
  data,
  target,
  source,
  libsizes = NULL,
  E = 3,
  k = E + 1,
  tau = 1,
  style = 1,
  lib = NULL,
  pred = NULL,
  boot = 99,
  replace = FALSE,
  seed = 42L,
  dist.metric = c("euclidean", "manhattan", "maximum"),
  zero.tolerance = max(k),
  relative = TRUE,
  weighted = TRUE,
  threads = length(libsizes),
  higher.parallel = TRUE,
  verbose = TRUE,
  detrend = FALSE,
  nb = NULL,
  ...
)

\S4method{pc}{SpatRaster}(
  data,
  target,
  source,
  libsizes = NULL,
  E = 3,
  k = E + 1,
  tau = 1,
  style = 1,
  lib = NULL,
  pred = NULL,
  boot = 99,
  replace = FALSE,
  seed = 42L,
  dist.metric = c("euclidean", "manhattan", "maximum"),
  zero.tolerance = max(k),
  relative = TRUE,
  weighted = TRUE,
  threads = length(libsizes),
  higher.parallel = TRUE,
  verbose = TRUE,
  detrend = FALSE,
  ...
)
}
\arguments{
\item{data}{Observation data.}

\item{target}{Integer of column indice for the target variable.}

\item{source}{Integer of column indice for the source variable.}

\item{libsizes}{(optional) Number of observations used.}

\item{E}{(optional) Embedding dimensions.}

\item{k}{(optional) Number of nearest neighbors used for projection.}

\item{tau}{(optional) Step of lag.}

\item{style}{(optional) Embedding style (\code{0} includes current state, \code{1} excludes it).}

\item{lib}{(optional) Libraries indices.}

\item{pred}{(optional) Predictions indices.}

\item{boot}{(optional) Number of bootstraps to perform.}

\item{replace}{(optional) Should sampling be with replacement?}

\item{seed}{(optional) Random seed.}

\item{dist.metric}{(optional) Distance measure to be used.}

\item{zero.tolerance}{(optional) Maximum number of zeros tolerated in signature space.}

\item{relative}{(optional) Whether to calculate relative changes in embedding.}

\item{weighted}{(optional) Whether to weight causal strength.}

\item{threads}{(optional) Number of threads used.}

\item{higher.parallel}{(optional) Whether to use a higher level of parallelism.}

\item{verbose}{(optional) Whether to show the progress bar.}

\item{h}{(optional) Prediction horizon.}

\item{...}{Additional arguments to absorb unused inputs in method dispatch.}

\item{detrend}{(optional) Whether to remove the linear trend.}

\item{nb}{(optional) Neighbours list.}
}
\value{
A list.
\describe{
\item{causality}{
A data.frame of causality results. When \code{libsizes} is \code{NULL}, it contains
per-sample causality estimates; otherwise, it contains causality results
evaluated across different library sizes.
}
\item{summary}{
A data.frame summarizing overall causality metrics. Only returned when
\code{libsizes} is \code{NULL}.
}
}
}
\description{
Pattern Causality
}
\examples{
crash = sf::read_sf(system.file("case/crash.gpkg", package = "pc"))
p1 = pc::pc(crash, 1, 2, E = 8, k = 8, threads = 1)
print(p1)
plot(p1)

# convergence diagnostics
p2 = pc::pc(crash, 1, 2, libsizes = seq(10,172,20), E = 8, k = 8, threads = 2)
print(p2)
plot(p2)

}
\references{
Stavroglou, S.K., Pantelous, A.A., Stanley, H.E., Zuev, K.M., 2020. Unveiling causal interactions in complex systems. Proceedings of the National Academy of Sciences 117, 7599–7605.
}
